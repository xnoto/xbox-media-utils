name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          version: "latest"

      - name: Install dependencies
        run: |
          uv sync --all-extras
          uv pip install pre-commit ruff

      - name: Run Ruff linter
        run: uv run ruff check --output-format=github .

      - name: Run Ruff formatter check
        run: uv run ruff format --check .

      # Type checking disabled for now - needs proper type annotations
      # - name: Run type checking
      #   run: uv run mypy src/xbox_media_utils

      - name: Run pre-commit hooks
        env:
          SKIP: no-commit-to-branch
        run: |
          uv run pre-commit install
          uv run pre-commit run --all-files --show-diff-on-failure

  commitizen:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install commitizen
        run: pip install commitizen

      - name: Check commit messages (PR)
        if: github.event_name == 'pull_request'
        run: cz check --rev-range origin/${{ github.base_ref }}..HEAD

      - name: Check commit messages (Push)
        if: github.event_name != 'pull_request'
        run: cz check --rev-range HEAD~1..HEAD
